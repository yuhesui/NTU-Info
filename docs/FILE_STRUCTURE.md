# File Structure

PROMPT_VERIFICATION: yhs

Target layout (in-progress):

- `public/` — static site output
  - `index.html`
  - `pages/` — tools pages
  - `styles/` — modular CSS
  - `scripts/` — modular JS (ES modules)
  - `data/` — local JSON data (no network required at runtime)

- `src/` — build/validation scripts
  - `utils/`
  - `scripts/`

- `docs/` — project documentation
